@page "/error/admin-api-connection"

<PageTitle>Admin API Connection Error</PageTitle>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-danger">
                <div class="card-header bg-danger text-white">
                    <h3 class="mb-0">
                        <i class="fa fa-exclamation-triangle me-2"></i>
                        Admin API Connection Error
                    </h3>
                </div>
                <div class="card-body">
                    <div class="text-center mb-4">
                        <i class="fa fa-server fa-4x text-danger mb-3"></i>
                        <h4>Unable to connect to the Admin API service</h4>
                    </div>

                    <div class="alert alert-info">
                        <p>The WebUI is unable to establish a connection with the Admin API service, which is required for most functionality.</p>
                    </div>

                    <h5 class="mt-4">Troubleshooting Steps:</h5>
                    <ol class="mt-3">
                        <li class="mb-2">Verify that the Admin API service is running and healthy</li>
                        <li class="mb-2">Check that the Admin API URL is correctly configured in the environment variables</li>
                        <li class="mb-2">Ensure that network connectivity between WebUI and Admin API services is working properly</li>
                        <li class="mb-2">Check the <code>CONDUIT_ADMIN_API_BASE_URL</code> environment variable is set correctly</li>
                        <li class="mb-2">Verify that the Admin API service is exposed on the expected port</li>
                        <li class="mb-2">Check logs for both WebUI and Admin API services for any error messages</li>
                    </ol>

                    <div class="d-grid gap-2 mt-4">
                        <button class="btn btn-primary" @onclick="RefreshPage">
                            <i class="fa fa-refresh me-2"></i> Try Again
                        </button>
                        <a href="/" class="btn btn-outline-secondary">
                            <i class="fa fa-home me-2"></i> Return to Home
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@code {
    private void RefreshPage()
    {
        // Force refresh the current page
        NavigationManager.NavigateTo(NavigationManager.Uri, forceLoad: true);
    }

    [Inject]
    private NavigationManager NavigationManager { get; set; } = null!;
}