@* Circuit breaker status component temporarily disabled due to missing health endpoint *@
@* TODO: Implement proper health check endpoint or use AdminApiClient for health checks *@

<div class="circuit-breaker-status">
    @* Component disabled - no health endpoint available *@
</div>

@code {
    // Component temporarily disabled to prevent exceptions
    // The original implementation was trying to call a non-existent health endpoint
    // To properly implement this:
    // 1. Add health checks to the WebUI project
    // 2. Or use the AdminApiClient to check Admin API health status
    // 3. Or remove this component if not needed
}