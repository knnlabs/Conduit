import type { FetchBaseApiClient } from '../client/FetchBaseApiClient';

/**
 * Type-safe Provider Models service using native fetch
 * @deprecated This service is deprecated as provider model endpoints no longer exist
 */
export class FetchProviderModelsService {
  constructor(private readonly client: FetchBaseApiClient) {}
  
  // This service is deprecated - all methods removed
}